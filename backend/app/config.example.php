<?php
/**
 * Конфигурация бэкенда. Скопировать в config.php и заполнить под сервер.
 * config.php НЕ коммитить и держать вне веб-доступа (он с секретами).
 */
return [
    // Абсолютные пути на сервере (без завершающего слэша).
    // data/ и baseline/ желательно ВЫШЕ public_html (см. DEPLOY.md §3).
    'DATA_DIR'     => __DIR__ . '/../data',       // база, uploads, cache, backups
    'BASELINE_DIR' => __DIR__ . '/baseline',      // эталонные HTML управляемых страниц
    'PUBLIC_DIR'   => __DIR__ . '/../public',     // веб-корень (для статики/снимков)

    // Публичный адрес сайта (для canonical/OG по умолчанию).
    'BASE_URL'     => 'https://iceberg.spb.ru',

    // Длинный случайный секрет: openssl rand -hex 32
    // Используется для подписи/шифрования секретов 2FA в базе.
    'APP_SECRET'   => 'ЗАМЕНИТЬ_НА_openssl_rand_hex_32',

    // Название в 2FA-приложении (что увидит маркетолог в Authenticator).
    'TOTP_ISSUER'  => 'ICEBERG Admin',

    // Безопасность сессии/входа.
    'SESSION_IDLE_TIMEOUT'     => 30 * 60,       // авто-выход после 30 мин простоя
    'SESSION_ABSOLUTE_TIMEOUT' => 12 * 3600,     // максимум 12 ч на сессию
    'LOGIN_MAX_ATTEMPTS'       => 5,             // попыток до блокировки
    'LOGIN_LOCK_WINDOW'        => 15 * 60,       // окно подсчёта попыток, сек
    'LOGIN_LOCK_BASE'          => 30,            // база нарастающей блокировки, сек

    // true = режим превью: полностраничный кэш ВЫКЛЮЧЕН (правки видно сразу, сайт медленнее).
    // На боевом — false. Гейт и noindex этим флагом больше не управляются: страницы приходят
    // из сборки уже в боевом виде.
    'PREVIEW' => false,

    // Разрешённые IP для входа в /admin (пусто = отключено; заполняется при выборе «доступ только с IP»).
    'ADMIN_IP_ALLOWLIST' => [],   // ['203.0.113.7', '198.51.100.0/24']

    // Журнал заявок, который пишет обработчик форм /send.php (см. mail-config.php).
    // Админка читает его и переносит заявки в базу. Пусто = ищем leads.log в веб-корне.
    'LEADS_LOG' => '',

    // Загрузка медиа.
    'UPLOAD_MAX_BYTES' => 8 * 1024 * 1024,       // 8 МБ на файл
    'UPLOAD_MAX_W'     => 3000,                  // максимальная ширина фото, px
    'UPLOAD_MAX_H'     => 3000,
];
